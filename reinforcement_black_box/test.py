import pandas as pd
import numpy as np

# a = {
#     'img1': 1 * np.ones((3,3)),
#     'img2': 2 * np.ones((3,3)),
#     'img3': 3 * np.ones((3,3)),
# }

# b = 3 * np.ones((3,3))

# # c = np.any([b == x for x in list(a.values())])

# finding = [x for x,y in a.items() if (y==b).all()]

# print(finding[0])

